import { styled } from "@phantomstudios/css-components";

import css from "./styles.module.css";

export const StatsWrapper = styled("div", {
  css: css.statsWrapper,
});

export const StatsCards = styled("ul", {
  css: css.statsCards,
});

export const StatCard = styled("li", {
  css: css.statCard,
});
